package com.gvtboard.prediction;
import android.content.Context;
import java.util.*;
public class NextWordPredictor {
    public NextWordPredictor(Context context) { }
    public List<String> predictNextWords(String lastWord) { return Arrays.asList("the", "and", "to", "of", "for"); }
}
